// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("beast", "for beast support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("beast", "enable beast support", "no");

if (PHP_BEAST != "no") {
	EXTENSION("beast", "beast.c");
}

